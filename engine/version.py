"""
The one place Meshwright's version number is written down.

The About panel reads it, and so does the build that stamps it onto Meshwright.exe and
into the installer — so a release cannot end up saying three different things.
"""
__version__ = "1.4.0"
