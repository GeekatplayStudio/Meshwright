# Meshwright — required packages (Geekatplay Studio)
#
# Install with install.bat / install.ps1, which puts these in an isolated .venv.
# Everything here ships ready-made wheels for every supported Python, so this
# file must install cleanly with no compiler on the machine. Anything that can
# fail to build lives in requirements-optional.txt and is installed separately.
#
# Version ranges are deliberately bounded: an unbounded numpy can silently
# upgrade a shared Python install and break unrelated projects (numba, pyarrow,
# torch and friends all pin numpy).

numpy>=1.26,<2.6
scipy>=1.11,<2
trimesh>=4.5,<6
pywebview>=5.0,<7
pillow>=10.0
lxml>=5.0                  # 3MF / DAE
shapely>=2.0               # 2D section helpers in trimesh
xatlas>=0.0.9              # UV unwrapping
rtree>=1.3,<2              # AABB queries behind exact UV transfer (bundles libspatialindex)
opencv-python>=4.8         # texture processing and seam dilation
